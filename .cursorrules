# UI状态控制器优化项目 - Cursor规则

## 🎯 项目概述
这是一个基于Cocos Creator的UI状态控制器系统优化项目，主要目标是提升功能性和用户体验。

### 核心组件
- `StateController.ts` - 状态控制器核心组件
- `StateSelect.ts` - 状态选择器组件  
- `StateEnum.ts` - 状态枚举定义

## 📋 优化指导原则

### 遵循README.md优化计划
严格按照README.md中定义的5个阶段优化路线图执行：
1. **第一阶段**：性能核心优化（内存、渲染性能）
2. **第二阶段**：架构重构优化（代码结构、数据流）
3. **第三阶段**：用户体验优化（编辑器体验、同步机制）
4. **第四阶段**：功能完善优化（高级功能、扩展性）
5. **第五阶段**：质量保证优化（测试、文档）

### Bug修复黄金原则
1. **精准命中**：定位问题根源，只修改必要的代码
2. **最小化影响**：避免对现有代码结构的大规模改动
3. **逻辑简明**：修复逻辑要简单明了，易于理解
4. **易于维护**：修复后的代码要便于后续维护

## 🔧 技术栈和架构信息

### 技术环境
- **引擎**: Cocos Creator 2.x
- **语言**: TypeScript
- **运行环境**: 编辑器模式 (CC_EDITOR)
- **装饰器**: @ccclass, @property, @executeInEditMode

### 核心架构
- **状态管理**: 基于枚举的状态系统
- **属性绑定**: 动态属性映射机制
- **数据结构**: 嵌套对象存储控制器数据
- **UI更新**: 实时属性同步机制

## 📝 代码修改指南

### 修改优先级
1. **🔴 高优先级**: 内存泄漏、性能瓶颈、严重bug
2. **🟡 中优先级**: 功能完善、用户体验提升
3. **🟢 低优先级**: 代码优化、文档完善

### 代码风格规范
- 使用 `let itself = this;` 模式
- 遵循驼峰命名法
- 使用TypeScript严格类型检查
- 保持原有的注释风格
- 函数内部优先使用 `itself` 而非 `this`

### 错误处理规范
- 使用 `CC_EDITOR` 条件检查编辑器环境
- 添加必要的空值检查
- 使用 `console.warn` 而非 `console.error` 进行警告
- 实现优雅的降级处理

## 🚫 修改约束

### 禁止操作
- 不要删除或大幅修改现有的数据结构
- 不要改变现有的类继承关系
- 不要修改装饰器的使用方式
- 不要破坏现有的编辑器集成功能

### 谨慎操作
- 修改属性访问器时要考虑编辑器显示
- 修改事件监听器时要确保正确清理
- 修改数据流时要保持向后兼容
- 添加新功能时要考虑性能影响

## 🔍 常见问题模式

### 数据完整性问题
- 检查 `EnumPropName.Non` (值为0) 的特殊处理
- 避免在propData中存储 `undefined` 值
- 确保 `$$lastProp$$` 不会被设置为无效值

### 性能相关问题
- 避免在高频调用中创建新对象
- 使用节流/防抖处理频繁更新
- 优化子节点遍历算法
- 减少不必要的DOM操作

### 内存泄漏风险
- 确保定时器被正确清理
- 检查事件监听器的注册和注销
- 避免循环引用
- 及时清理临时对象

## 🧪 测试和验证

### 修改后必须验证
1. 功能是否正常工作
2. 性能是否有改善
3. 是否引入新的bug
4. 是否影响其他功能

### 测试场景
- 多个状态切换
- 属性同步功能
- 编辑器集成功能
- 大量数据时的性能表现

## 💡 最佳实践示例

### 好的修改示例
```typescript
// ✅ 精准修复，只修改必要的逻辑
if (value !== EnumPropName.Non) {
    propData.$$lastProp$$ = value;
    // 其他逻辑...
}
```

### 避免的修改示例
```typescript
// ❌ 过度修改，引入复杂的清理逻辑
function cleanupAllData() {
    // 复杂的清理逻辑...
}
```

## 📚 代码理解要点

### 关键概念
- `_ctrlData`: 存储所有控制器状态数据的核心结构
- `$$lastProp$$`: 记录上次选择的属性，用于状态恢复
- `$$changedProp$$`: 记录已修改的属性，用于UI显示
- `$$default$$`: 默认状态属性值

### 数据流向
1. 用户选择属性 → `propKey` setter
2. 获取属性值 → `handleValue` 方法
3. 存储到数据结构 → `_ctrlData`
4. 状态切换时 → `updateState` 方法
5. 应用到UI → `updateUI` 方法

## 🎨 UI编辑器集成

### 编辑器相关特性
- `@executeInEditMode()` 启用编辑器模式
- `cc.Class.Attr.setClassAttr` 动态设置属性
- 编辑器属性面板的动态更新
- 实时预览功能

### 编辑器兼容性
- 确保所有修改在编辑器中正常工作
- 保持属性面板的响应性
- 维护实时预览功能
- 支持撤销/重做操作

## 🔄 持续优化

### 性能监控
- 关注内存使用情况
- 监控UI更新频率
- 检查事件监听器数量
- 评估数据结构效率

### 代码质量
- 定期review代码复杂度
- 检查类型安全
- 验证错误处理完整性
- 确保代码可读性

---

**遵循这些规则，确保每次修改都能精准解决问题，同时保持代码的整洁和可维护性。** 